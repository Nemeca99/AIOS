ARCHIVE_SEED v0.1 - 'Root Spark'

This is the seed package for the offline Archive AI body. It contains no intelligence by itself, but serves as the core recursive learning container. All memory evolves over time. Logic is isolated and non-destructive. All processes are local.