apiVersion: apps/v1
kind: Deployment
metadata:
  name: carma-af-deployment
  namespace: carma-af
  labels:
    app: carma-mycelium
    region: AF
spec:
  replicas: 100
  selector:
    matchLabels:
      app: carma-mycelium
      region: AF
  template:
    metadata:
      labels:
        app: carma-mycelium
        region: AF
    spec:
      containers:
      - name: carma-block
        image: carma-mycelium:latest
        ports:
        - containerPort: 443
        - containerPort: 80
        env:
        - name: REGION
          value: AF
        - name: TOTAL_BLOCKS
          value: "19,047,619"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 30
